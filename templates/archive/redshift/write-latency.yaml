id: redshift-write-latency
name: "Redshift High Write Latency"
description: "Alert when Redshift write latency is high"
service: redshift
severity: warning

data_sources:
  cloudwatch:
    type: cloudwatch
    namespace: AWS/Redshift
    metric: WriteLatency
    statistic: Average
    dimensions:
      - ClusterIdentifier
    period: 300

defaults:
  threshold: 100
  threshold_operator: gt
  evaluation_interval: 5m
  for_duration: 5m

labels:
  service: redshift
  category: performance

annotations:
  summary: "High write latency on Redshift {{ $labels.resource_name }}"
  description: "Redshift cluster {{ $labels.resource_name }} write latency is {{ $value }}ms"

customizable:
  - threshold
  - evaluation_interval
  - for_duration
  - labels
  - contact_point
  - severity
